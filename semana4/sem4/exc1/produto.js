// produto.js exportar função chamada calcularPreco que recebe preço e quantidade
export const taxaImposto = 0.1;

export function calcularPreco(preco, quantidade) {
  return (precoTotal = preco * quantidade);
}
